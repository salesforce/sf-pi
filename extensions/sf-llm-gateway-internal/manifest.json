{
  "id": "sf-llm-gateway-internal",
  "name": "SF LLM Gateway",
  "description": "Salesforce LLM Gateway provider with model discovery",
  "category": "provider",
  "maturity": "stable",
  "defaultEnabled": true,
  "configurable": true,
  "commands": ["/sf-llm-gateway"],
  "providers": ["sf-llm-gateway-internal"],
  "events": [
    "session_start",
    "turn_end",
    "model_select",
    "after_provider_response",
    "session_shutdown"
  ],
  "docs": {
    "summary": "Complete Pi Provider for the Salesforce LLM Gateway. Pi-owned credential persistence and model storage, static baseline plus dynamic overlay, real mixed-API dispatch, Gateway-aware transport adapters, explicit refresh, diagnostics, and usage status.",
    "primaryFiles": [
      "index.ts",
      "lib/provider.ts",
      "lib/transport.ts",
      "lib/models.ts",
      "lib/model-resolution.ts",
      "lib/stale-usage-refresh.ts",
      "lib/config.ts",
      "lib/config-panel.ts",
      "lib/monthly-usage.ts",
      "lib/doctor.ts"
    ],
    "stateFiles": [
      "~/.pi/agent/auth.json (Pi-owned API-key credential + default non-secret URL)",
      "<globalAgentDir>/sf-llm-gateway-internal.json (non-secret settings; legacy apiKey is migration-only)",
      "<globalAgentDir>/sf-llm-gateway/* (monthly usage and diagnostic state)"
    ],
    "safety": [
      "API-key input uses a fixed-mask SF Pi component and never enters Pi's visible stock prompt.",
      "Pi alone persists/removes credentials; setup and import paths write no secrets.",
      "Pi's settings.json is mutated through pi-settings.ts helpers with race-aware reads."
    ]
  }
}
