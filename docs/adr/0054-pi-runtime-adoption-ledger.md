---
id: "0054"
status: accepted
date: 2026-06-17
---

# ADR 0054: Pi Runtime Adoption Ledger

When Pi Runtime releases add features that overlap with SF Pi, SF Pi will use a **Pi Runtime Adoption Ledger** instead of copying every release note into code. The ledger records whether SF Pi should adopt, defer, ignore, or delete overlapping code for each runtime capability, plus the owning surface and the behavior proof required.

This keeps SF Pi simple and Pi-native: Pi owns runtime concerns such as trust, tool selection, proxy settings, session names, and footer indicators; SF Pi consumes those surfaces only when they improve Salesforce workflows.

## Current ledger

| Pi Runtime capability                                                            | SF Pi response                                                                                                                                                                                                                                                                                                                                                                                                      | Owning surface                                                            | Behavior proof                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ctx.mode` and RPC UI protocol                                                   | Adopt for command/UI routing. Custom TUI requires `ctx.mode === "tui"`; RPC uses Pi dialog/notification methods; print/JSON stay text-first.                                                                                                                                                                                                                                                                        | `lib/common/command-panel.ts`, command-bearing extensions                 | Mode tests show TUI, RPC, and headless behavior without calling `ctx.ui.custom()` outside TUI.                                                                                                                   |
| Project Trust and `ctx.isProjectTrusted()`                                       | Adopt as the only trust boundary for project-local Pi inputs. Do not add an SF Pi trust model.                                                                                                                                                                                                                                                                                                                      | `sf-skills`, `/sf-pi skills`, future project-local readers                | Trusted/untrusted tests prove project-local sources are hidden or blocked when untrusted.                                                                                                                        |
| Pi active-tool selection and `--exclude-tools`                                   | Adopt as authoritative. SF Pi may narrow active tools but must not re-enable excluded tools.                                                                                                                                                                                                                                                                                                                        | `sf-brain`, `sf-slack`, tool-gating helpers                               | Regression tests prove excluded extension tools stay inactive after SF Pi scope changes.                                                                                                                         |
| Pi 0.80.7 dynamic tool loading                                                   | Evaluate **Progressive Tool Activation** through the bounded SF Browser pilot in ADR 0083 after active-branch context is correct. Do not add a package-wide router or custom protocol flags without measured proof.                                                                                                                                                                                                 | `sf-browser`, future high-cardinality tool families                       | Tests prove additive next-turn loading, fallback models, exclusions, Guardrail, resume/compaction, and measurable schema/cache benefit.                                                                          |
| Provider-scoped `auth.json.env` and global `httpProxy`                           | Defer code changes; document Pi-native configuration instead of adding SF Pi proxy/env settings.                                                                                                                                                                                                                                                                                                                    | `sf-llm-gateway` docs/help                                                | Config tests prove existing gateway precedence remains stable and no duplicate proxy setting appears.                                                                                                            |
| Package asset/path helpers                                                       | Adopt when replacing asset-only package-root discovery; keep explicit package-filter path comparisons where needed.                                                                                                                                                                                                                                                                                                 | Shared catalog/state helpers, manager surfaces                            | Linked-install and package-install tests resolve bundled assets without path-walking regressions.                                                                                                                |
| Pi 0.79.9 runtime floor                                                          | Adopt as a runtime-floor bump. Update package metadata, runtime gate, lockfile, docs, and tests together; do not mix gateway model behavior into this slice.                                                                                                                                                                                                                                                        | `package.json`, `lib/common/pi-compat.ts`, repo docs                      | Version-floor tests prove the metadata and runtime gate agree.                                                                                                                                                   |
| Pi 0.79.10 runtime floor                                                         | Adopt as a narrow runtime-floor bump per ADR 0059. Keep compaction metadata ledger-only, simplify update guidance, and make reload-workaround deletion test-first.                                                                                                                                                                                                                                                  | `package.json`, `lib/common/pi-compat.ts`, repo docs                      | Version-floor tests prove metadata and runtime gate agree; targeted tests cover any deleted reload workaround.                                                                                                   |
| Pi 0.80.2 runtime floor                                                          | Adopt as a clean runtime-floor bump per ADR 0066. Remove local streamer shims, use Pi's documented `pi-ai/compat` entrypoint for legacy stream functions, and keep neutral helper imports on root packages.                                                                                                                                                                                                         | `package.json`, `lib/common/pi-compat.ts`, `sf-llm-gateway`               | Version-floor tests prove metadata and runtime gate agree; gateway transport tests prove static compat imports stay stable.                                                                                      |
| Pi 0.80.6 runtime floor                                                          | Adopt as a **Runtime Floor Adoption Slice** per ADR 0076 before using 0.80.4+ extension APIs. Keep the slice limited to metadata, lockfile, runtime gate, docs, and version-floor tests.                                                                                                                                                                                                                            | `package.json`, `package-lock.json`, `lib/common/pi-compat.ts`, repo docs | Version-floor tests prove metadata and runtime gate agree; no feature behavior changes ride in the floor bump.                                                                                                   |
| Pi 0.81 provider-owned authentication and removal of `ModelRegistry.authStorage` | Adopt natively. Gateway, Docs, and Slack use one shared, fixed-mask SF Pi provider UI while Pi alone owns persistence/logout. Existing API-key and OAuth-compatible credentials plus environment automation remain usable; no extension accesses private auth storage. Interactive entry is TUI-only; other modes retain existing/env auth paths.                                                                   | `sf-docs`, `sf-slack`, `sf-llm-gateway`                                   | Exact Pi TUI and Models tests prove fixed masking, no terminal/session/config token echo, mode-`0600` persistence, native logout, credential-shape compatibility, and no production `.authStorage`; env remains. |
| Pi `0.x` compatibility range                                                     | Adopt ADR 0079's **hard floor, soft audit ceiling** policy. Stable `>=0.84.0 <1.0.0` is loadable; releases above the **Pi Runtime Audit Edge** run in forward-compatibility mode, while older, prerelease, and Pi 1.x runtimes remain blocked. Pi 0.84.4 is the recommended exact runtime and latest audited edge.                                                                                                  | `package.json`, `lib/common/pi-compat.ts`, CI, `sf-pi-manager`            | Package metadata matches the hard range; required CI covers the 0.84.0 floor and 0.84.4 audited edge, while a non-blocking `latest` canary reports future drift.                                                 |
| Pi 0.83 audit edge                                                               | Adopted Pi 0.83 as an earlier **Pi Runtime Audit Edge** without raising the 0.82.0 **Pi Runtime Floor**; ADR 0100 removes provider-specific terminal workarounds and relies on native Pi stream semantics.                                                                                                                                                                                                          | `sf-llm-gateway`, `package.json`, `lib/common/pi-compat.ts`, CI           | Exact 0.82.0 and 0.83.0 typechecks and full suites prove the provider-neutral adapters preserve terminal coherence.                                                                                              |
| Pi 0.84 audit edge                                                               | Adopt Pi 0.84.4 as the latest **Pi Runtime Audit Edge** with the 0.84.0 **Pi Runtime Floor** (ADR 0109). Inherit the 0.84.4 runtime pin; do not add SF Pi Modules for `defaultTools`, `expandPromptTemplates`, fullscreen search, Cloudflare gateway bindings, the PowerShell tool, installer-managed updates, or `session_compact_failed` HUD. Gateway discovery uses typed 0.84 `ModelRegistry.refresh(options)`. | `sf-llm-gateway`, `package.json`, `lib/common/pi-compat.ts`, CI           | Exact 0.84.0/0.84.4 typecheck/full suite plus existing Gateway proofs; no new product surface.                                                                                                                   |
| Pi 0.84.4 `ui_prompt_start` / `ui_prompt_end`                                    | Defer runtime adoption to a separate deletion proof. The audit-edge bump adds no event handlers; a follow-up may centralize custom-prompt working visibility and remove local hide/show pairs only after exact TUI behavior is proven.                                                                                                                                                                              | `sf-pi-manager`, `sf-skills`, `sf-agentscript`                            | Future proof covers prompt completion, cancellation, failure, nesting, TUI-only behavior, and loader restoration before deleting local wrappers.                                                                 |
| Pi 0.84.4 terminal capability overrides                                          | Adopt Pi as the sole owner of hyperlink, image-protocol, and truecolor override settings. Add no SF Pi duplicate. A separate support-hardening slice may consume Pi's resolved truecolor capability for SF Pi-owned raw ANSI rendering.                                                                                                                                                                             | Pi settings; future `lib/common/color-policy.ts`                          | The audit-edge bump relies on Pi's tests; a future behavior proof must cover forced truecolor off/on while preserving `NO_COLOR` and theme-managed 256-color output.                                             |
| Pi 0.84.4 public image MIME detector, RPC queue clearing, and fullscreen copying | Keep these Pi-owned. The MIME helper does not replace SF tldraw's descriptor-bound screenshot security checks; SF Pi owns no RPC client queue; fullscreen selection behavior needs no SF Pi wrapper.                                                                                                                                                                                                                | Pi Runtime; `sf-tldraw` validator remains local                           | Exact 0.84.4 typecheck/full suite; no new SF Pi behavior or deletion in this slice.                                                                                                                              |
| Pi 0.84.4 provider, compaction, session, and large-render fixes                  | Inherit through the exact Pi family pins. Gateway adapters continue delegating to `pi-ai/compat`; bounded Salesforce result digests and artifacts remain because Pi compaction is lossy and threshold-driven.                                                                                                                                                                                                       | Pi Runtime, `sf-llm-gateway`, Salesforce family tools                     | Exact 0.84.4 full suite plus existing Gateway retry/transport and artifact-first proofs.                                                                                                                         |
| Pi 0.83 `ctx.scopedModels`                                                       | Defer. The 0.84.0 floor no longer blocks the API, but Gateway exclusive/additive `enabledModels` remains SF Pi product logic. Do not add a version branch merely to mirror Pi's scoped-model picker.                                                                                                                                                                                                                | Future model-selection work                                               | A future parity proof must cover static, cached, and discovered Gateway models, enabled-model patterns, pinned thinking levels, and the empty-means-unrestricted contract before deleting local behavior.        |
| Pi 0.83 per-request `fetch` injection                                            | Deleted the unused `SF_LLM_GATEWAY_TRACE` process-global `fetch` wrapper instead of adopting per-request fetch injection. Do not add a generic fetch Module or dual runtime paths. Diagnostics remain doctor, usage-probe, and Pi-owned logs.                                                                                                                                                                       | `sf-llm-gateway`                                                          | Source and runtime tests prove Gateway no longer wraps `global fetch` or reads `SF_LLM_GATEWAY_TRACE`.                                                                                                           |
| Pi 0.83 `pending` and raw provider stop reasons                                  | Adopt the terminal-coherence contract, not a new status feature. Partials may use `pending`; terminal events/messages never may. Preserve optional raw reasons when native transports provide them, while normalized `stopReason` remains authoritative for control flow and errors.                                                                                                                                | `sf-llm-gateway`                                                          | SF Pi tests cover partial `pending`, synthetic stop/tool/length coherence, and no finalized `pending`; Pi's native provider contract owns error/abort and unknown raw-reason mapping.                            |
| Pi credential output and auth readiness commands                                 | Keep CLI ownership in Pi. SF Pi does not call or wrap the credential-output commands; in-process integrations use Provider authentication. SF Guardrail mediates model-issued `pi auth print-*` and `pi auth check --credentials` calls, while ordinary `pi auth check` remains allowed.                                                                                                                            | Pi CLI; `sf-guardrail`                                                    | Safety Kernel tests cover direct, common-wrapper, environment-prefixed, and `npx` credential output plus non-output readiness checks.                                                                            |
| Active branch and `context` event projection                                     | Implemented **Active-Branch Context Projection** per ADR 0080. Immutable guidance counts only while live on the active compaction-aware branch; mutable hidden context contributes only its latest value. The append-only audit record is preserved.                                                                                                                                                                | `lib/common/session`, `sf-brain`, `sf-devbar`, `sf-slack`, `sf-guardrail` | Behavior tests cover sibling branches, tree navigation, compaction, resume, fork, and A→B→A without filtering state or audit entries.                                                                            |
| `appendEntry()` + `registerEntryRenderer()`                                      | Adopted for **Human-Only Transcript Rows** under the **Agent Workflow Visibility Contract** in ADR 0085. Gateway and Feedback display-only reports no longer use `sendMessage()`; agent-visible follow-ups remain only for actionable findings.                                                                                                                                                                     | `sf-lsp`, `sf-code-analyzer`, `sf-feedback`, `sf-llm-gateway`             | Exact-Pi mode tests prove TUI/RPC/JSON/print visibility, state-only entry emission, and exclusion of newly emitted reports from later LLM context.                                                               |
| `agent_settled`                                                                  | Adopt for **Agent-Settled Quality Gates** and the **Agent-Settled Update Coordinator** only after Pi has no automatic retry, compaction retry, or queued follow-up left.                                                                                                                                                                                                                                            | `sf-code-analyzer`, `sf-pi-manager`                                       | Behavior tests prove changed files scan once after settlement; due updates remain pending until settlement, recheck consent/idle state, abort on a new turn, and never overlap.                                  |
| Pi-owned assistant retry lifecycle                                               | Adopted per ADR 0095. Pi owns assistant retry policy, backoff, cancellation, and `auto_retry_start` / `auto_retry_end` visibility. SF Pi deleted its Anthropic attempt loop, fixed sleeps, Gateway retry default, private event bus, and listener wiring; only Gateway error normalization and terminal guidance remain.                                                                                            | `sf-llm-gateway`                                                          | Exact Pi 0.84.0/0.84.4 tests cover transient and partial failures, disabled retry, exhaustion, cancellation, attempts, and error normalization; a scoped live proof covers all three transport families.         |
| Gateway thinking levels                                                          | Neutral authenticated metadata may advertise reasoning support, while Pi/user settings own active selection. ADR 0100 removes exact-model advanced-level maps; SF Pi writes no thinking default and calls no ExtensionAPI thinking setter.                                                                                                                                                                          | `sf-llm-gateway`, themes                                                  | Exact Pi tests prove user settings remain Pi-owned and discovered models receive no inferred advanced levels.                                                                                                    |
| Pi tool-result lifecycle shapes                                                  | Adopt Pi's exported `ToolResultEvent.input` for successful SF Herdr workflow inference. Do not invent `args` on `tool_execution_end` or double-count the same execution.                                                                                                                                                                                                                                            | `sf-herdr`                                                                | Exported-shape and exact-runtime tests cover Agent Script, Data 360, Apex, LWC, Browser, Herdr, write/edit, event order, validated input, and failed-result exclusion.                                           |
| Input-based pricing tiers                                                        | Defer for SF LLM Gateway. Gateway-provided usage remains authoritative; do not synthesize local tier pricing from model IDs.                                                                                                                                                                                                                                                                                        | `sf-llm-gateway`                                                          | Future tests require neutral authenticated pricing metadata before changing zero-cost model definitions.                                                                                                         |
| `before_provider_headers`                                                        | SF Pi does not own model-specific beta headers. ADR 0100 keeps protocol adapters provider-neutral instead of maintaining a duplicate header system.                                                                                                                                                                                                                                                                 | `sf-llm-gateway`                                                          | Adapter tests prove requests pass through without SF Pi-owned model-specific headers.                                                                                                                            |
| Pi 0.81 complete Provider extensions                                             | Implemented as the **Complete Gateway Provider** per ADR 0081. Pi owns credential persistence/logout, model storage, refresh retention, and API-map dispatch; legacy cache, re-registration, pseudo-auth, and ID dispatch are deleted with no dual production path.                                                                                                                                                 | `sf-llm-gateway`                                                          | Real Pi masked login/logout, URL persistence, env/legacy precedence, offline bootstrap, refresh retention, model overrides, three live transports, compaction, retries, and network-free startup all pass.       |
| CLI-equivalent model resolver SDK exports                                        | Adopt through **Model Resolution Delegation** when gateway parity tests pass, even if the slice is riskier, because generic model/scoped-model parsing belongs to Pi.                                                                                                                                                                                                                                               | `sf-llm-gateway`                                                          | Parity tests prove defaults, discovered models, diagnostics, scoped models, and thinking shorthand still work.                                                                                                   |
| Project-local resource configuration                                             | Evaluate through **Native Resource Delegation** and a **Resource Resolution Parity Proof** per ADR 0082. Decide each SF Skills capability separately; delete only proven native duplication and retain Salesforce-specific leverage.                                                                                                                                                                                | `sf-pi-manager`, `sf-skills`, package-state helpers                       | Pi's real resolver and Funnel agree across override, delta, path, conflict, trust, stale-root, and rescope scenarios before any production behavior is removed.                                                  |
| `showCacheMissNotices`                                                           | Defer to Pi-owned troubleshooting guidance. Do not build a duplicate SF DevBar cache-miss notice system.                                                                                                                                                                                                                                                                                                            | `sf-devbar`, docs/help only if needed                                     | No runtime tests unless SF Pi later consumes a stable Pi cache indicator.                                                                                                                                        |
| Direct-provider model releases                                                   | Ignore for SF LLM Gateway unless neutral authenticated gateway metadata reports the capability. Direct-provider aliases never authorize gateway routing or payload policy.                                                                                                                                                                                                                                          | `sf-llm-gateway`                                                          | Discovery evidence may populate neutral fields only; exact route and payload tests are prohibited by ADR 0100.                                                                                                   |
| Compaction `reason` / `willRetry` metadata                                       | Defer runtime behavior, including Pi 0.84.3 `session_compact_failed`. Record the capability for future diagnostics, but do not add HUD/status noise until a concrete Salesforce workflow needs it.                                                                                                                                                                                                                  | Future compaction-aware diagnostics                                       | No runtime tests until adopted; future tests must cover manual, threshold, overflow retry, and failed compaction events.                                                                                         |
| Pi-native update flow                                                            | Let Pi own runtime updates. Welcome offers newer stable 0.x releases, Doctor reports forward compatibility without recommending a downgrade, and ADR 0084's coordinator continues to skip self-update mutation. Exact 0.84.4 repair remains for blocked old, prerelease, or major runtimes.                                                                                                                         | `sf-welcome`, `sf-pi-manager`, `lib/common/pi-compat.ts`                  | Tests prove future stable releases load with one warning, blocked runtimes retain exact repair guidance, and the coordinator never invokes Pi self-update.                                                       |
| Reload/session replacement UI fixes                                              | Use as a deletion opportunity only after proof. Try removing local reload deferrals, especially in `sf-skills`, with tests or manual TUI QA before committing deletion.                                                                                                                                                                                                                                             | `sf-skills` first, other reload-sensitive panels if found                 | Reload behavior proof shows no stranded overlays and input remains blocked only while reload is active.                                                                                                          |
| Plan-mode example workflow fixes                                                 | Adopt the pattern, not a new bundled extension. Ordered SF Pi workflows should use **Workflow-Local Plan Tracking** where it clarifies progress.                                                                                                                                                                                                                                                                    | `sf-herdr`, `sf-code-analyzer`, Agent Script/Data 360 flows               | Workflow-specific tests prove steps are visible and follow-ups queue correctly without adding a package-wide plan mode.                                                                                          |
| `find` nested git repository boundary fix                                        | Ignore for SF Pi code unless a local workaround is discovered. Trust Pi's built-in find behavior rather than wrapping it.                                                                                                                                                                                                                                                                                           | None                                                                      | No tests needed unless deleting a discovered workaround.                                                                                                                                                         |
| OpenAI-compatible encrypted `reasoning_details` stream preservation              | Rely on upstream Pi streaming. Do not add an SF LLM Gateway workaround unless a gateway-served model produces a concrete regression.                                                                                                                                                                                                                                                                                | `sf-llm-gateway`                                                          | Existing transport tests remain enough; add a live or fixture regression only if gateway evidence appears.                                                                                                       |
| Removed selective-provider base entry points                                     | Adopt by deleting the old base-import posture. Keep root package imports for neutral helpers and use `pi-ai/compat` for legacy stream functions until a provider-factory migration is separately proven.                                                                                                                                                                                                            | Tool-schema modules, gateway transport modules                            | Typecheck proves no removed base references remain; gateway transport tests prove stream imports work.                                                                                                           |
| Chat-template thinking compatibility                                             | Defer until a gateway-served model actually requires `chat_template_kwargs`; keep model behavior changes in a separate live-validated slice.                                                                                                                                                                                                                                                                        | `sf-llm-gateway`                                                          | Gateway model/payload tests prove only the validated model family receives chat-template kwargs.                                                                                                                 |
| Direct-provider model metadata releases                                          | Ignore for SF LLM Gateway unless the model appears in gateway discovery or explicit gateway configuration. Pi direct-provider aliases are not gateway adoption signals.                                                                                                                                                                                                                                             | `sf-llm-gateway`                                                          | Gateway discovery/metadata evidence precedes any gateway model definition, routing, or payload test.                                                                                                             |
| Pi-native footer/cache/context indicators                                        | SF DevBar consumes only public Pi facts. It preserves nullable `ctx.getContextUsage().percent`, shows explicit post-compaction unknown state, and does not clone Pi 0.81 usage/cache accounting.                                                                                                                                                                                                                    | `sf-devbar`, `sf-llm-gateway` status surfaces                             | Adapter tests prove absent/unknown/zero/fractional context states, public model/thinking/session facts, bounded narrow rendering, and no local usage/cache parser.                                               |
| Session display names                                                            | Adopted passively in SF DevBar through Pi's public getter/event. SF Pi does not create a session naming system or derive fallback names.                                                                                                                                                                                                                                                                            | `sf-devbar`                                                               | Display tests show bounded Pi-owned names when present and unchanged rendering when absent.                                                                                                                      |
| `ctx.getSystemPromptOptions()`                                                   | Defer unless a command has a narrow diagnostic need; treat contents as sensitive.                                                                                                                                                                                                                                                                                                                                   | Future diagnostics only                                                   | Tests must prove no context file contents or secrets are exposed.                                                                                                                                                |
| `InputEvent.streamingBehavior`                                                   | Ignore until a real input-routing need exists.                                                                                                                                                                                                                                                                                                                                                                      | None                                                                      | No tests needed until adopted.                                                                                                                                                                                   |
| Pi Runtime release notes                                                         | Ignore in SF Pi surfaces except freshness/update guidance.                                                                                                                                                                                                                                                                                                                                                          | `sf-welcome`                                                              | Release-status tests prove SF Pi does not render upstream Pi changelog bullets.                                                                                                                                  |

### Shared credential-prompt outcome

ADR 0087 supersedes the temporary Docs/Slack containment posture without waiting for a Pi release. Gateway, Docs, and Slack use one shared fixed-mask provider UI; Pi remains the sole credential persistence/logout owner. Existing API-key and OAuth-compatible credentials plus environment automation remain supported, and no extension accesses private auth storage.

### Dynamic-tool outcome

The Browser progressive-activation pilot was stopped before implementation. SF Pi retains Pi's normal eager tool activation and authoritative exclusions; no loader, router, deferred-provider flag, or activation state landed.

### M3B credential-cutoff outcome

Gateway authentication uses Pi-owned credentials or canonical environment variables only. ADR 0101 removes compatibility aliases and config-token cleanup; credentials and state under prior identities remain untouched, and users reconnect explicitly through the canonical provider.

### E4 resource-parity outcome

Pi 0.81.1 matches the Funnel for global inheritance, additive top-level project scope, project-only loading, project trust, missing-root omission, known default-root precedence, and one-skill rescope results. Package deltas, exact filters, complete package collision inventory, and whole-source rescope remain explicit decision rows. SF-specific lifecycle, discovery, stale-wiring diagnostics, rescope convenience, and usage awareness remain retained leverage. E4 authorizes no production deletion or fix.

## Runtime Delegation / Simplification Backlog

1. **Code Analyzer human-only transcript rows** — landed. Use `appendEntry()` and `registerEntryRenderer()` for auto-scan status rows; keep agent-visible follow-ups as messages.
2. **Provider-neutral Gateway headers** — landed. SF Pi no longer owns model-specific beta-header toggles, headers, or live route probes.
3. **Slack/SF Docs auth status delegation** — landed. Execution paths use Pi auth storage through `ctx`; status-only surfaces share a tokenless `lib/common/pi-auth-status.ts` fallback.
4. **Gateway settings I/O reuse** — landed. Gateway Pi settings mutations delegate tolerant JSON file I/O to `lib/common/sf-pi-settings.ts` while keeping gateway-specific config logic local.
5. **Apex/SOQL Salesforce REST adapter consolidation** — move generic REST/Tooling transport helpers behind `lib/common/sf-conn` / `lib/common/sf-rest`; keep lifecycle semantics local.
6. **Lifecycle shell cleanup** — split into command shell catalog reuse, common artifact writer, then result-card adapter.
7. **Package filter/package-root consolidation** — landed. Package source/filter interpretation lives in `lib/common/sf-pi-package-resolution.ts`; package-root lookup uses Pi's package manager through `lib/common/sf-pi-package-root.ts` with a dev-run fallback.
8. **Org Status Fallback helper** — landed. Fallback selection lives in `lib/common/sf-environment/display-fallback.ts`; DevBar rendering remains local.

## Consequences

Future release audits should prefer small adoption/deletion slices with behavior tests over broad runtime wrappers. If a Pi Runtime feature raises a deeper product question, such as credential ownership, it gets its own design pass instead of being folded into release-note cleanup.
