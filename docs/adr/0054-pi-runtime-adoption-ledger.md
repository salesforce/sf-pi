# ADR 0054: Pi Runtime Adoption Ledger

Status: accepted

When Pi Runtime releases add features that overlap with SF Pi, SF Pi will use a **Pi Runtime Adoption Ledger** instead of copying every release note into code. The ledger records whether SF Pi should adopt, defer, ignore, or delete overlapping code for each runtime capability, plus the owning surface and the behavior proof required.

This keeps SF Pi simple and Pi-native: Pi owns runtime concerns such as trust, tool selection, proxy settings, session names, and footer indicators; SF Pi consumes those surfaces only when they improve Salesforce workflows.

## Current ledger

| Pi Runtime capability                                               | SF Pi response                                                                                                                                                                                              | Owning surface                                                            | Behavior proof                                                                                                                |
| ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `ctx.mode` and RPC UI protocol                                      | Adopt for command/UI routing. Custom TUI requires `ctx.mode === "tui"`; RPC uses Pi dialog/notification methods; print/JSON stay text-first.                                                                | `lib/common/command-panel.ts`, command-bearing extensions                 | Mode tests show TUI, RPC, and headless behavior without calling `ctx.ui.custom()` outside TUI.                                |
| Project Trust and `ctx.isProjectTrusted()`                          | Adopt as the only trust boundary for project-local Pi inputs. Do not add an SF Pi trust model.                                                                                                              | `sf-skills`, `/sf-pi skills`, future project-local readers                | Trusted/untrusted tests prove project-local sources are hidden or blocked when untrusted.                                     |
| Pi active-tool selection and `--exclude-tools`                      | Adopt as authoritative. SF Pi may narrow active tools but must not re-enable excluded tools.                                                                                                                | `sf-brain`, `sf-slack`, tool-gating helpers                               | Regression tests prove excluded extension tools stay inactive after SF Pi scope changes.                                      |
| Provider-scoped `auth.json.env` and global `httpProxy`              | Defer code changes; document Pi-native configuration instead of adding SF Pi proxy/env settings.                                                                                                            | `sf-llm-gateway-internal` docs/help                                       | Config tests prove existing gateway precedence remains stable and no duplicate proxy setting appears.                         |
| Package asset/path helpers                                          | Adopt when replacing asset-only package-root discovery; keep explicit package-filter path comparisons where needed.                                                                                         | Shared catalog/state helpers, manager surfaces                            | Linked-install and package-install tests resolve bundled assets without path-walking regressions.                             |
| Pi 0.79.9 runtime floor                                             | Adopt as a runtime-floor bump. Update package metadata, runtime gate, lockfile, docs, and tests together; do not mix gateway model behavior into this slice.                                                | `package.json`, `lib/common/pi-compat.ts`, repo docs                      | Version-floor tests prove the metadata and runtime gate agree.                                                                |
| Pi 0.79.10 runtime floor                                            | Adopt as a narrow runtime-floor bump per ADR 0059. Keep compaction metadata ledger-only, simplify update guidance, and make reload-workaround deletion test-first.                                          | `package.json`, `lib/common/pi-compat.ts`, repo docs                      | Version-floor tests prove metadata and runtime gate agree; targeted tests cover any deleted reload workaround.                |
| Pi 0.80.2 runtime floor                                             | Adopt as a clean runtime-floor bump per ADR 0066. Remove local streamer shims, use Pi's documented `pi-ai/compat` entrypoint for legacy stream functions, and keep neutral helper imports on root packages. | `package.json`, `lib/common/pi-compat.ts`, `sf-llm-gateway`               | Version-floor tests prove metadata and runtime gate agree; gateway transport tests prove static compat imports stay stable.   |
| Pi 0.80.6 runtime floor                                             | Adopt as a **Runtime Floor Adoption Slice** per ADR 0076 before using 0.80.4+ extension APIs. Keep the slice limited to metadata, lockfile, runtime gate, docs, and version-floor tests.                    | `package.json`, `package-lock.json`, `lib/common/pi-compat.ts`, repo docs | Version-floor tests prove metadata and runtime gate agree; no feature behavior changes ride in the floor bump.                |
| `appendEntry()` + `registerEntryRenderer()`                         | Adopt for **Human-Only Transcript Rows** and deprecate display-only `sendMessage()` rows where the contract is model-invisible human awareness. Start with SF LSP before broader migration.                 | `sf-lsp` first; later Code Analyzer rows only after intent review         | Behavior tests prove rows render for humans and do not enter LLM context.                                                     |
| `agent_settled`                                                     | Adopt for **Agent-Settled Quality Gates** that must wait until Pi has no automatic retry, compaction retry, or queued follow-up left. Start with Code Analyzer deferred auto-scan.                          | `sf-code-analyzer`                                                        | Behavior tests prove changed files are scanned once after the agent settles and not during automatic continuation.            |
| `max` thinking level                                                | Adopt through model-specific **Gateway Thinking Capability** only. Do not make `max` the default and do not assume universal support across gateway models.                                                 | `sf-llm-gateway-internal`, themes                                         | Gateway thinking tests prove supported models expose `max`, unsupported models do not, and `xhigh` compatibility remains.     |
| Input-based pricing tiers                                           | Defer for SF LLM Gateway. Respect **Gateway Spend Authority**; do not synthesize local tier pricing unless gateway metadata explicitly provides trustworthy tier data.                                      | `sf-llm-gateway-internal`                                                 | Future tests prove zero-cost gateway model metadata remains unless valid gateway tier metadata is passed through.             |
| `before_provider_headers`                                           | Prove first with a **Gateway Header Proof Spike**; if equivalent and simpler, replace the older gateway header path promptly instead of keeping both systems.                                               | `sf-llm-gateway-internal`                                                 | Header tests prove routing, model defaults, beta headers, setting changes, and secret redaction remain correct.               |
| CLI-equivalent model resolver SDK exports                           | Adopt through **Model Resolution Delegation** when gateway parity tests pass, even if the slice is riskier, because generic model/scoped-model parsing belongs to Pi.                                       | `sf-llm-gateway-internal`                                                 | Parity tests prove defaults, discovered models, diagnostics, scoped models, and thinking shorthand still work.                |
| Project-local resource configuration                                | Adopt through conservative **Native Resource Delegation**. Do not remove curated Salesforce manager/skills UX yet and do not weaken project-local safety rules.                                             | `sf-pi-manager`, `sf-skills`, package-state helpers                       | Scope tests prove global/project enablement stays correct and trusted project-local resources cannot weaken safety.           |
| `showCacheMissNotices`                                              | Defer to Pi-owned troubleshooting guidance. Do not build a duplicate SF DevBar cache-miss notice system.                                                                                                    | `sf-devbar`, docs/help only if needed                                     | No runtime tests unless SF Pi later consumes a stable Pi cache indicator.                                                     |
| Direct-provider GPT-5.6 and provider metadata releases              | Continue to ignore for SF LLM Gateway unless gateway discovery or explicit gateway configuration proves the model IDs and behavior.                                                                         | `sf-llm-gateway-internal`                                                 | Gateway discovery/metadata evidence precedes any preset, routing, or payload test.                                            |
| Compaction `reason` / `willRetry` metadata                          | Defer runtime behavior. Record the capability for future diagnostics, but do not add HUD/status noise until a concrete Salesforce workflow needs it.                                                        | Future compaction-aware diagnostics                                       | No runtime tests until adopted; future tests must cover manual, threshold, and overflow retry compaction events.              |
| Safer exact-version `pi update` flow                                | Adopt in guidance through the **Pi Runtime Update Happy Path**: normal surfaces show `pi update --self --force`; install-specific npm repair details stay behind `/sf-pi doctor`.                           | `sf-welcome`, `sf-pi-manager`, `lib/common/pi-compat.ts`                  | Copy/render tests prove normal surfaces use the single Pi-native command and doctor retains advanced evidence.                |
| Reload/session replacement UI fixes                                 | Use as a deletion opportunity only after proof. Try removing local reload deferrals, especially in `sf-skills`, with tests or manual TUI QA before committing deletion.                                     | `sf-skills` first, other reload-sensitive panels if found                 | Reload behavior proof shows no stranded overlays and input remains blocked only while reload is active.                       |
| Plan-mode example workflow fixes                                    | Adopt the pattern, not a new bundled extension. Ordered SF Pi workflows should use **Workflow-Local Plan Tracking** where it clarifies progress.                                                            | `sf-herdr`, `sf-code-analyzer`, Agent Script/Data 360 flows               | Workflow-specific tests prove steps are visible and follow-ups queue correctly without adding a package-wide plan mode.       |
| `find` nested git repository boundary fix                           | Ignore for SF Pi code unless a local workaround is discovered. Trust Pi's built-in find behavior rather than wrapping it.                                                                                   | None                                                                      | No tests needed unless deleting a discovered workaround.                                                                      |
| OpenAI-compatible encrypted `reasoning_details` stream preservation | Rely on upstream Pi streaming. Do not add an SF LLM Gateway workaround unless a gateway-served model produces a concrete regression.                                                                        | `sf-llm-gateway-internal`                                                 | Existing transport tests remain enough; add a live or fixture regression only if gateway evidence appears.                    |
| Removed selective-provider base entry points                        | Adopt by deleting the old base-import posture. Keep root package imports for neutral helpers and use `pi-ai/compat` for legacy stream functions until a provider-factory migration is separately proven.    | Tool-schema modules, gateway transport modules                            | Typecheck proves no removed base references remain; gateway transport tests prove stream imports work.                        |
| Chat-template thinking compatibility                                | Defer until a gateway-served model actually requires `chat_template_kwargs`; keep model behavior changes in a separate live-validated slice.                                                                | `sf-llm-gateway-internal`                                                 | Gateway model/payload tests prove only the validated model family receives chat-template kwargs.                              |
| Direct-provider model metadata releases                             | Ignore for SF LLM Gateway unless the model appears in gateway discovery or explicit gateway configuration. Pi direct-provider aliases are not gateway adoption signals.                                     | `sf-llm-gateway-internal`                                                 | Gateway discovery/metadata evidence precedes any gateway preset, routing, or payload test.                                    |
| Pi-native footer/cache/context indicators                           | Defer to a separate DevBar slice. Keep DevBar as the default Salesforce footer, but explore passing through Pi-owned runtime indicators instead of hiding or reimplementing them.                           | `sf-devbar`, `sf-llm-gateway-internal` status surfaces                    | DevBar rendering tests prove Salesforce/Gateway rows remain while Pi-native model, context, and cache indicators are visible. |
| Session display names                                               | Adopt passively for display only. Do not create an SF Pi session naming system.                                                                                                                             | `sf-welcome`, `sf-devbar` if useful                                       | Display tests show Pi-owned names when present and existing fallbacks when absent.                                            |
| `ctx.getSystemPromptOptions()`                                      | Defer unless a command has a narrow diagnostic need; treat contents as sensitive.                                                                                                                           | Future diagnostics only                                                   | Tests must prove no context file contents or secrets are exposed.                                                             |
| `InputEvent.streamingBehavior`                                      | Ignore until a real input-routing need exists.                                                                                                                                                              | None                                                                      | No tests needed until adopted.                                                                                                |
| Pi Runtime release notes                                            | Ignore in SF Pi surfaces except freshness/update guidance.                                                                                                                                                  | `sf-welcome`                                                              | Release-status tests prove SF Pi does not render upstream Pi changelog bullets.                                               |

## Consequences

Future release audits should prefer small adoption/deletion slices with behavior tests over broad runtime wrappers. If a Pi Runtime feature raises a deeper product question, such as credential ownership, it gets its own design pass instead of being folded into release-note cleanup.
