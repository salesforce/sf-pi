node_modules
.pi
coverage/
catalog/index.json
catalog/registry.ts
reports/dependency-license-report.md
reports/licenses-all.json
reports/licenses-production.json

# Vendored @agentscript/agentforce bundle — do not reformat.
# It's a prebuilt upstream artifact synced by scripts/sync-agentforce-sdk.mjs.
extensions/sf-agentscript-assist/lib/vendor/

# CHANGELOG.md is generated by release-please and doesn't need Prettier.
# Formatting it breaks release-please's own parser on the next release.
CHANGELOG.md

# package-lock.json is generated by npm and should not be reformatted.
package-lock.json

# Salesforce Operator Kernel body is injected verbatim into the system prompt.
# Prettier interprets indentation, `__c`/`__r` suffixes, and fenced blocks as
# Markdown markup, which mangles the wording. Keep it pristine.
extensions/sf-brain/SF_KERNEL.md

# Salesforce OSPO boilerplate files. Keep byte-identical with the
# salesforce/oss-template upstream so they round-trip cleanly on future
# OSPO refreshes. Our edits to CONTRIBUTING and SECURITY are formatted
# by prettier already (they're our content, not OSPO verbatim).
CODE_OF_CONDUCT.md
how_to_license.md
LICENSE.txt
